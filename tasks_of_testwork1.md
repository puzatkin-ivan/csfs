## Проверочная работа

Для написания программ используем редактор `Kate`, который можно найти через `Пуск` во вкладке `Офис`
Для запуска программ используем `Терминал`, который можно найти через `Пуск` во вкладке `Системные`

**План действий**
1. Открываем редактор `Kate`
2. Читаем задание
3. Пишем программу в редакторе `Kate`
4. Сохраняем файл с названием `task<номер задания>.py`, где `<номер задания>` - порядковый номер задания(Например, для Задания №1 название должно быть `task1.py`)
5. Открываем `Терминал Fly`
6. Запускаем программу с помощью **python3** (Например, для Задания №1 запуск будет выглядеть как `python3 task1.py`)
7. Проверяем работу программы
8. В случае, если программа удовлетворяет требованиям, переходим к следующему заданию

### Задания №1
Написать программу, которая выводит на экран
```bash
Hello Kitty!
```

### Задания №2
Написать программу, которая выводит на экран елочку
```bash
    *
   ***
  *****
 *******
*********
```
`*` - в основании елочки 9 звездочек

### Задания №3
Написать программу, в которую пользователь вводит своё имя, и программа выводит текст `<User name> is the best`
```sh
Ivan
Ivan is the best
```

### Задания №4
Написать программу, в которую пользователь вводит 3 числа и выдает сумму трех чисел
> Не забываем! Функция `input()` возвращает строку. Для того, чтобы сложить числа, нужно преобразовать строку в число с помощью функции `int()`
> Можно сделать так: int(input())

### Задания №5
Написать программу, которая читает пользовательский ввод с клавиатуры

### Задания №6
Написать программу, которая читает пользовательский ввод с клавиатуры
